// Root layout, fonts, metadata.
// Placeholder file for architectural scaffold.

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>
}
